# Руководство по работе с SEO-запросами

## Описание

Система управления SEO-запросами позволяет импортировать, кластеризировать и отображать на сайте все поисковые запросы пользователей. Все запросы автоматически распределяются по кластерам (группам) и создаются отдельные страницы для каждого запроса.

## Как использовать

### 1. Массовый импорт запросов

1. Зайдите в админ-панель → **SEO Запросы**
2. Откройте раздел **"📥 Массовый импорт SEO-запросов"**
3. Вставьте список запросов из таблицы (каждый запрос с новой строки)
4. Нажмите **"✅ Импортировать запросы"**

**Пример:**
```
Двигатель ММЗ Д-245
Двигатель ММЗ Д-245.12С-1
Двигатель ММЗ Д-245.12С-2
Двигатель ММЗ Д-245.12С-3
...
```

### 2. Автоматическая кластеризация

Система автоматически группирует запросы по кластерам:

- **ММЗ Д-245.12С-1, ММЗ Д-245.12С-2, ...** → Кластер: **"ММЗ Д-245.12С"**
- **Двигатель ММЗ Д-245** → Кластер: **"ММЗ Д-245"**
- **Двигатель для ГАЗель** → Кластер: **"Двигатель для ГАЗель"**

### 3. Структура URL

После импорта запросы доступны по следующим URL:

- **Отдельный запрос:** `/query/dvigatel-mmz-d-245-12s-1`
- **Кластер запросов:** `/query-cluster/ММЗ Д-245.12С`

### 4. Редактирование запросов

1. Найдите нужный запрос в списке
2. Раскройте его для редактирования
3. Измените:
   - Meta Title
   - Meta Description
   - Meta Keywords
   - H1 заголовок
   - Контент страницы
   - Приоритет (1-10)
   - Статус (Активен/Черновик/Архивирован)

### 5. Статистика

В разделе **"📊 Статистика запросов"** вы можете увидеть:
- Количество запросов в каждом кластере
- Общее количество активных запросов

## Автоматические функции

### Генерация контента

При импорте система автоматически генерирует:
- **Meta Title:** "Купить [Запрос] в Алматы | Контрактные двигатели | Motor Land"
- **Meta Description:** Описание с ключевыми словами
- **H1:** "Купить [Запрос] в Алматы"
- **Контент:** Базовый текст с преимуществами покупки

### Интеграция с sitemap.xml

Все активные запросы автоматически добавляются в `sitemap.xml`:
- Кластеры: приоритет 0.7, обновление weekly
- Отдельные запросы: приоритет 0.5-0.8 (зависит от приоритета запроса), обновление monthly

## Структура базы данных

Таблица `seo_queries` содержит:
- `query_text` - текст запроса
- `slug` - URL-friendly идентификатор
- `cluster` - кластер (группа запросов)
- `meta_title`, `meta_description`, `meta_keywords` - SEO мета-теги
- `h1_text` - заголовок страницы
- `content` - контент страницы (HTML)
- `priority` - приоритет (1-10)
- `status` - статус (active/draft/archived)

## Рекомендации

1. **Импортируйте запросы порциями** - не более 1000 за раз для стабильной работы
2. **Проверяйте кластеризацию** - при необходимости корректируйте кластеры вручную
3. **Редактируйте контент** - автоматически сгенерированный контент нужно доработать для лучшего SEO
4. **Устанавливайте приоритеты** - важные запросы должны иметь приоритет 8-10
5. **Используйте статусы** - черновики не попадают в sitemap, архивированные скрыты от пользователей

## Примеры использования

### Импорт из Excel/Google Sheets

1. Скопируйте колонку с запросами из таблицы
2. Вставьте в поле импорта (каждый запрос с новой строки)
3. Нажмите "Импортировать"

### Создание уникального контента

После импорта отредактируйте каждый важный запрос:
- Добавьте уникальный текст
- Используйте ключевые слова естественным образом
- Добавьте структурированные данные (списки, таблицы)
- Включите призыв к действию

## Техническая информация

- **Файлы модуля:** `hyst/mods/seo_queries/`
- **Страница запроса:** `seo_query.php`
- **Страница кластера:** `seo_cluster.php`
- **URL маршрутизация:** Настроена в `.htaccess`

