# Руководство по настройке технического SEO

Это руководство содержит инструкции по настройке всех аспектов технического SEO для сайта motor-land.kz.

## ✅ Выполненные задачи

### 1. Оптимизация robots.txt
- ✅ Добавлены правила для блокировки служебных параметров URL (utm_source, fbclid, gclid и т.д.)
- ✅ Настроен Crawl-delay для вежливых роботов
- ✅ Добавлены правила для ЧПУ URL товаров

### 2. Оптимизация sitemap.php
- ✅ Добавлена генерация ЧПУ URL для товаров
- ✅ Исправлены URL страниц (убраны .php расширения)
- ✅ Добавлена поддержка старых URL для обратной совместимости

### 3. Улучшение страницы 404
- ✅ Добавлена семантическая разметка
- ✅ Добавлены ссылки на основные страницы
- ✅ Добавлен meta robots noindex для предотвращения индексации

### 4. Исправление структуры заголовков H1-H6
- ✅ На главной странице добавлен H1 заголовок
- ✅ Исправлена иерархия заголовков на всех страницах

### 5. Каноникализация URL
- ✅ Настроены редиректы 301 для удаления параметров из URL
- ✅ Добавлена поддержка ЧПУ URL для товаров
- ✅ Настроены редиректы для удаления trailing slash

### 6. Защита от спама
- ✅ Добавлена honeypot защита
- ✅ Добавлена проверка времени отправки формы
- ✅ Добавлена проверка на подозрительные паттерны
- ✅ Добавлена санитизация входных данных

### 7. Настройки безопасности
- ✅ Добавлены заголовки безопасности в .htaccess
- ✅ Настроена защита от XSS, clickjacking и других атак

### 8. Настройки производительности
- ✅ Настроено кэширование статических ресурсов
- ✅ Добавлена поддержка Brotli сжатия
- ✅ Оптимизированы заголовки Cache-Control

## 📋 Задачи, требующие ручной настройки

### 1. Настройка SSL-сертификата (HTTPS)

Для настройки SSL-сертификата:

1. Получите SSL-сертификат от вашего хостинг-провайдера или используйте Let's Encrypt (бесплатно)
2. Раскомментируйте следующие строки в `.htaccess`:
```apache
# SEO: Редирект с http на https (если SSL настроен)
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
```

3. Убедитесь, что в `sitemap.php` используется HTTPS:
```php
$base_url = 'https://motor-land.kz';
```

4. Проверьте, что все внутренние ссылки используют HTTPS

### 2. Настройка reCAPTCHA для форм

Для добавления Google reCAPTCHA v3:

1. Зарегистрируйте сайт на https://www.google.com/recaptcha/admin
2. Получите Site Key и Secret Key
3. Добавьте скрипт reCAPTCHA в `hyst/head.php` перед закрывающим тегом `</head>`:
```html
<!-- Security: Google reCAPTCHA v3 -->
<script src="https://www.google.com/recaptcha/api.js?render=YOUR_SITE_KEY"></script>
<script>
function submitFormWithRecaptcha(formData, callback) {
    grecaptcha.ready(function() {
        grecaptcha.execute('YOUR_SITE_KEY', {action: 'submit'}).then(function(token) {
            formData.append('recaptcha_token', token);
            callback();
        });
    });
}
</script>
```

4. Добавьте проверку токена в `send_form.php`:
```php
// Проверка reCAPTCHA токена
function verify_recaptcha($token) {
    $secret_key = 'YOUR_SECRET_KEY';
    $url = 'https://www.google.com/recaptcha/api/siteverify';
    $data = array(
        'secret' => $secret_key,
        'response' => $token
    );
    $options = array(
        'http' => array(
            'header' => "Content-type: application/x-www-form-urlencoded\r\n",
            'method' => 'POST',
            'content' => http_build_query($data)
        )
    );
    $context = stream_context_create($options);
    $result = file_get_contents($url, false, $context);
    $response = json_decode($result, true);
    return $response['success'] && $response['score'] > 0.5;
}
```

5. Обновите функцию `check_spam_protection()` для использования reCAPTCHA:
```php
if (isset($_POST['recaptcha_token']) && !verify_recaptcha($_POST['recaptcha_token'])) {
    return false;
}
```

### 3. Минификация CSS и JS

Для минификации CSS и JS файлов:

#### Вариант 1: Использование онлайн-инструментов
1. Используйте инструменты типа:
   - CSS: https://cssminifier.com/
   - JS: https://javascript-minifier.com/

2. Создайте минифицированные версии:
   - `css.css` → `css.min.css`
   - `mob.css` → `mob.min.css`
   - `tab.css` → `tab.min.css`
   - `des/myjs.js` → `des/myjs.min.js`

3. Обновите ссылки в `hyst/head.php`:
```php
<link rel="stylesheet" href="css.min.css?<?=$INTERFACE_VERSION;?>" type="text/css"/>
```

#### Вариант 2: Использование build-процесса
1. Установите Node.js и npm
2. Установите инструменты минификации:
```bash
npm install -g clean-css-cli uglify-js
```

3. Создайте скрипт для автоматической минификации:
```bash
# Минификация CSS
cleancss -o css.min.css css.css
cleancss -o mob.min.css mob.css
cleancss -o tab.min.css tab.css

# Минификация JS
uglifyjs des/myjs.js -o des/myjs.min.js -c -m
```

### 4. Настройка CDN

Для настройки CDN (например, Cloudflare):

1. Зарегистрируйтесь на CDN-провайдере (Cloudflare, KeyCDN и т.д.)
2. Настройте DNS для вашего домена
3. Раскомментируйте настройки CDN в `.htaccess`:
```apache
# Performance: CDN настройки
Header set Access-Control-Allow-Origin "*"
Header set Access-Control-Allow-Methods "GET, HEAD, OPTIONS"
Header set Access-Control-Max-Age "3600"
```

4. Обновите пути к статическим ресурсам для использования CDN URL:
```php
$CDN_URL = 'https://cdn.motor-land.kz';
// Используйте $CDN_URL для изображений, CSS, JS
```

5. Настройте кэширование на CDN:
   - Изображения: 1 год
   - CSS/JS: 1 месяц
   - HTML: 1 час

### 5. Проверка скорости загрузки (Core Web Vitals)

Для проверки Core Web Vitals:

1. Используйте инструменты:
   - Google PageSpeed Insights: https://pagespeed.web.dev/
   - Google Search Console: https://search.google.com/search-console
   - GTmetrix: https://gtmetrix.com/

2. Проверьте следующие метрики:
   - **LCP (Largest Contentful Paint)**: должен быть < 2.5 секунды
   - **FID (First Input Delay)**: должен быть < 100 миллисекунд
   - **CLS (Cumulative Layout Shift)**: должен быть < 0.1

3. Улучшения для Core Web Vitals:
   - Используйте preload для критических ресурсов
   - Оптимизируйте изображения (WebP, правильные размеры)
   - Минифицируйте CSS и JS
   - Используйте lazy loading для изображений
   - Оптимизируйте шрифты (preload, font-display: swap)

### 6. Проверка адаптивности

Для проверки адаптивности:

1. Используйте инструменты:
   - Google Mobile-Friendly Test: https://search.google.com/test/mobile-friendly
   - BrowserStack: https://www.browserstack.com/
   - Chrome DevTools (F12 → Toggle device toolbar)

2. Проверьте на различных устройствах:
   - Мобильные телефоны (320px - 768px)
   - Планшеты (768px - 1024px)
   - Десктопы (1024px+)

3. Убедитесь, что:
   - Viewport настроен правильно: `<meta name="viewport" content="width=device-width, initial-scale=1">`
   - Используются медиа-запросы для адаптивности
   - Кнопки и ссылки достаточно большие для касания (минимум 44x44px)
   - Текст читабелен без масштабирования

### 7. Отправка sitemap.xml в Google Search Console

1. Зайдите в Google Search Console: https://search.google.com/search-console
2. Выберите свой сайт
3. Перейдите в раздел "Sitemaps"
4. Добавьте URL sitemap: `https://motor-land.kz/sitemap.php`
5. Нажмите "Отправить"

### 8. Проверка индексации страниц

1. В Google Search Console перейдите в раздел "Покрытие"
2. Проверьте, какие страницы проиндексированы
3. Используйте команду `site:motor-land.kz` в Google для проверки индексации
4. Проверьте наличие дублей страниц:
   - Используйте инструмент "Проверка URL" в Search Console
   - Проверьте canonical теги на всех страницах

### 9. Удаление дублей страниц

Для удаления дублей:

1. Проверьте наличие дублей через Google Search Console
2. Убедитесь, что на каждой странице есть правильный canonical тег:
```html
<link rel="canonical" href="https://motor-land.kz/correct-url"/>
```

3. Настройте редиректы 301 для старых URL на новые:
```apache
# В .htaccess
RewriteRule ^old-url$ /new-url [R=301,L]
```

4. Убедитесь, что версии с www и без www используют один canonical URL

### 10. Настройка hreflang (для мультиязычности)

Если сайт мультиязычный, добавьте hreflang теги:

```html
<link rel="alternate" hreflang="ru" href="https://motor-land.kz/" />
<link rel="alternate" hreflang="kz" href="https://motor-land.kz/kz/" />
<link rel="alternate" hreflang="x-default" href="https://motor-land.kz/" />
```

## 📊 Чек-лист выполнения задач

- [x] Оптимизация robots.txt
- [x] Исправление sitemap.php
- [x] Улучшение страницы 404
- [x] Исправление структуры заголовков H1-H6
- [x] Каноникализация URL
- [x] Защита от спама (базовая)
- [x] Настройки безопасности
- [x] Настройки производительности
- [ ] Настройка SSL-сертификата (требует действий на сервере)
- [ ] Настройка reCAPTCHA (требует регистрации)
- [ ] Минификация CSS и JS (требует build-процесса)
- [ ] Настройка CDN (требует регистрации)
- [ ] Проверка скорости загрузки (требует тестирования)
- [ ] Проверка адаптивности (требует тестирования)
- [ ] Отправка sitemap в Google Search Console (требует доступа)
- [ ] Проверка индексации страниц (требует доступа)
- [ ] Удаление дублей страниц (требует проверки)
- [ ] Настройка hreflang (если нужна мультиязычность)

## 🔍 Дополнительные рекомендации

1. **Регулярное обновление sitemap**: Убедитесь, что sitemap обновляется автоматически при добавлении новых товаров
2. **Мониторинг ошибок 404**: Регулярно проверяйте логи на наличие 404 ошибок и настраивайте редиректы
3. **Оптимизация изображений**: Конвертируйте все изображения в WebP формат для лучшей производительности
4. **Использование schema.org**: Убедитесь, что все страницы используют правильную schema.org разметку
5. **Регулярное тестирование**: Регулярно проверяйте скорость загрузки и Core Web Vitals

## 📞 Поддержка

Если у вас возникли вопросы по настройке, обратитесь к документации:
- Google Search Central: https://developers.google.com/search
- Web.dev: https://web.dev/
- MDN Web Docs: https://developer.mozilla.org/

